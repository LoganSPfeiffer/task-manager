# Styling Rules

- ALL styling via Tailwind utility classes in className
- No custom CSS rules — globals.css only has `@import "tailwindcss"`
- No inline style={{}} — always className
- Verify Tailwind class names are correct before using
